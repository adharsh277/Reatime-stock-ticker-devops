FROM mcr.microsoft.com/devcontainers/base:ubuntu

# Install Docker and general tools
RUN apt-get update && apt-get install -y \
    curl \
    gnupg \
    lsb-release \
    software-properties-common
